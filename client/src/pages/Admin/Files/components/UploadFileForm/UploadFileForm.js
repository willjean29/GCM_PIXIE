//Componente para Modal de Carga de Archivos
import React from 'react';
import {Modal, Layout, Card, Table, Tag, Space, Button } from 'antd' // Esto sirve para importar los componentes

import './UploadFileForm.scss';
const UploadFileForm = (props) => {
  const {Content} = Layout;
  return (  
    <Layout>
      <Content>
      
        <Card>
         
        </Card>
      </Content>
    </Layout>
  );
}
 
export default UploadFileForm;